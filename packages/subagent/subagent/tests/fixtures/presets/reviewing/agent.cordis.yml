# A second agent-plane composition, so the named-preset join is a real switch:
# the tool a joined child sees has to change with it.
- id: only
  name: ../../plugins/preset-tool.js
  config:
    tool: reviewing_only
